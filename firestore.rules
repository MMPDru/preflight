rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // AUTHENTICATION DISABLED - ALLOW ALL ACCESS
    // All collections are now publicly accessible for read and write
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
